// Session services are supervised by dinit (user instance, child of niri).
// Definitions live in ~/.config/dinit.d/. Only dinit is spawned here; it brings
// up pipewire, wireplumber, pipewire-pulse, noctalia and the dbus-env one-shot.
spawn-at-startup "dinit-session"
