# pipewire-pulse - PulseAudio compatibility layer on top of pipewire.
# Started after pipewire; auto-restarts on crash.
type = process
command = /usr/bin/pipewire-pulse
restart = true
smooth-recovery = true
depends-on: pipewire
