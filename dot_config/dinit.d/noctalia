# noctalia - Wayland shell / wallpaper / session UI daemon.
# Supervised by dinit; auto-restarts on crash.
type = process
command = /usr/bin/noctalia
restart = true
smooth-recovery = true
waits-for: dbus-env
