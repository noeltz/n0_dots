# System services to enable (runit: symlinked into /var/service). Idempotent.
enable_system = [
  "backlight",
  "dbus",
  "libvirtd",
  "virtlockd",
  "virtlogd",
]

# System services to disable and stop (runit: removed from /var/service, sv down). Idempotent.
disable_system = [

]
