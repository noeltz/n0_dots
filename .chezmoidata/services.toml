# User services wired into the niri session (systemctl --user add-wants niri.service)
niri_wants = [

]

# System services to enable (systemctl enable). Idempotent.
enable_system = [

]

# System services to disable and stop (systemctl disable --now). Idempotent.
disable_system = [

]

# User services to enable (systemctl --user enable). Idempotent.
enable_user = [
  "app-com.mitchellh.ghostty.service",
]

# User services to disable and stop (systemctl --user disable --now). Idempotent.
disable_user = [

]
